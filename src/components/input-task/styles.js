import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputContainer: {
    flexDirection: "row",
  },
  input: {
    flex: 0.95,
    borderWidth: 1,
    borderRadius: 10,
    paddingHorizontal: 10,
    color: "#212121",
    height: 40,
    fontSize: 14,
  },
  inputHeaderText: {
    fontWeight: "bold",
    fontSize: 15,
  },
});
